---
type: log
tags: [log]
updated: 2026-06-03
---

# Ingest Log

Chronological record of all sources ingested into this wiki.

---

- 2026-05-26: **bootstrap** → All initial pages created from codebase knowledge (CLAUDE.md, hesai_xt16.yaml, imu.yaml, cyclonedds.xml, git history, memory). Pages: overview, go2-robot, hesai-xt16, fast-lio2, nav2, go2-nav-bridge, orin-dock, imu, tf-tree, slam, motion-undistortion, extrinsics, fast-lio2-params, dds-config.
- 2026-05-26: **PTP sync configuration** → Verifica live Pandar Console: PTP Free Run → Tracking con `ptp4l -i eth0 -S`. Integrato in `ros_entrypoint.sh` + `linuxptp` in Dockerfile runtime. Creata `concepts/ptp-sync.md`; aggiornata `entities/hesai-xt16.md`.
- 2026-05-26: **PTP arch refactor** → `ptp4l` rimosso da container Docker (`ros_entrypoint.sh` + `linuxptp` da Dockerfile). Orin host fa da PTP master su `eth0`. Ragione: conflitto BMCA tra host e container su stessa interfaccia fisica. Aggiornate `concepts/ptp-sync.md` e `entities/hesai-xt16.md`.
- 2026-05-26: **PTP LOCKED↔TRACKING oscillation analysis** → Causa: SW timestamping varianza + CLOCK_REALTIME Orin non disciplinato. Impatto FAST-LIO2 tabulato per offset µs. Fix: NTP su Orin host. Aggiornata `concepts/ptp-sync.md`.
- 2026-06-01: **workflow — offline SLAM via rosbag record/replay** → Formalizzato il pattern record-once-replay-many per sviluppare SLAM senza robot fisico. Principio cardine: registrare topic grezzi a monte (`/lidar_points`, `/utlidar/robot_odom`, `/utlidar/imu`, `/tf_static`), NON quelli derivati (`/scan`, `/tf`, `/map`) — un solo bag rigioca sia per slam_toolbox_2d sia per FAST-LIO2. Coppia critica `--clock` + `use_sim_time:=true`. Pagina creata: `concepts/offline-slam-replay.md`. Aggiornato: `_index.md`.
- 2026-06-03: **lab session — offline 2D map validation + tuning** → (1) Scoperto difetto **clock-domain**: header stamp di `/lidar_points` (system clock Orin non-synced, ~8568 s) vs `/utlidar/robot_odom` (UTC MCU, ~1.78e9 s) differiscono ~56 anni → slam_toolbox scarta il 100 % degli scan. Fix offline `bags/restamp_bag.py` (header.stamp = recv-time); fix proprio = sync clock Orin. (2) **Tuning** su `slam2d_timefixed`: `HuberLoss` + `scan_buffer_size 30` + slab 0.0–0.45 + `resolution 0.03` → mappa più fine, drift `map→odom` yaw −2.9°. (3) **Regressi misurati**: levelling dello slab via `base_stabilized` (attitude leg-odom troppo rumorosa, serve IMU); ring-decimation a rings 7,8 (height-band già fa ring-gating a distanza). Infra aggiunta: `scan_preprocessor` (NEW), `base_stabilized` in `odom_tf_broadcaster`. Pagine create: `concepts/clock-domain-mismatch.md`, `concepts/2d-map-tuning.md`. Aggiornate: `entities/slam-toolbox-2d.md`, `concepts/offline-slam-replay.md`, `_index.md`.
- 2026-06-03: **feature — IMU-levelled scan slab (gait tilt compensation)** → Risolve [[slam-toolbox-2d]] open-risk #2. `odom_tf_broadcaster` ora pubblica `base_link→base_stabilized` da `/utlidar/imu` (gravità, drift-bounded) invece dell'attitude leg-odom biased (≈−2.6° roll) che era regredita in [[2d-map-tuning]]. Path: firmware-quaternion se presente, altrimenti **complementary filter** (gyro HF + accel-gravity LF, `α=τ/(τ+dt)`, `cf_tau=0.25 s` → crossover ~0.64 Hz). Yaw scartato (no magnetometro). Offset statici `roll_offset`/`pitch_offset` da calibrazione a livella. `pointcloud_to_laserscan` retargetato `target_frame: base_stabilized`; slam_toolbox resta `base_frame: base_link`. Caveat replay: `/utlidar/imu` è MCU-domain → serve bag re-stamped + walking bag per validare ([[clock-domain-mismatch]]). Build pulita (distrobox `humble_ws`). Pagina creata: `concepts/imu-tilt-compensation.md` (thesis-grade: sensor model, derivazioni, filtro, frame math, validazione). Aggiornate: `entities/slam-toolbox-2d.md`, `entities/imu.md`, `concepts/2d-map-tuning.md`, `config/pointcloud_to_laserscan.yaml`, launch `mapping_2d{,_replay}.launch.py`, `_index.md`.
- 2026-05-29: **lab session — FAST-LIO2 Z runaway → pivot SLAM 2D** → (1) Fix campo per-punto `time` (driver Hesai ROS2 emette `time` FLOAT32, non `timestamp` FLOAT64): regressione 82c756e/4bc8fcc rivertita. (2) Diagnosi divergenza Z: osservabilità verticale nulla del 16-line orizzontale (FOV 30°) → bias accel integra → runaway quadratico; x/y/yaw ok. Bag `/tmp/standup_bag`,`/tmp/walk_bag`. (3) Consiglio 3 subagent + NotebookLM → decisione: SLAM 2D. Pagine create: `concepts/z-observability.md`, `entities/slam-toolbox-2d.md`. Aggiornate: `concepts/slam.md`, `concepts/tf-tree.md`, `entities/fast-lio2.md`, `_index.md`.
